"use client";

import ShopControls from "./ShopControls";

export default function SecondarySections({ lang }) {
  return (
    <ShopControls
      categories={[]}
      filters={{}}
      lang={lang}
      variant="sidebar"
    />
  );
}
